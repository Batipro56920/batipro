// TODO: extraction complète à faire après séparation du state matériel.
export default function IntervenantEquipmentSection({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}

