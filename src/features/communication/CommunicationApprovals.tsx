import { useCallback, useEffect, useState } from "react";
import { CheckCircle2, Clock3, MessageSquareWarning, RefreshCw, Send } from "lucide-react";
import { Button } from "../../components/ui/button";
import { decidePublication, listReviewPublications } from "./communicationRepository";
import type { ReviewPublication, SocialNetwork } from "./types";

const labels:Record<SocialNetwork,string>={facebook:"Facebook",instagram:"Instagram",linkedin:"LinkedIn",google_business:"Google Business",tiktok:"TikTok",youtube:"YouTube"};
export function CommunicationApprovals(){
 const [items,setItems]=useState<ReviewPublication[]>([]);const [selected,setSelected]=useState<string|null>(null);const [note,setNote]=useState("");const [busy,setBusy]=useState(false);const [message,setMessage]=useState("");
 const reload=useCallback(async()=>{try{const rows=await listReviewPublications();setItems(rows);setSelected(current=>rows.some(row=>row.id===current)?current:rows[0]?.id??null);}catch{setItems([]);}},[]);useEffect(()=>{void reload();},[reload]);
 const item=items.find(row=>row.id===selected)??null;
 async function decide(decision:"approved"|"changes_requested"){if(!item)return;setBusy(true);setMessage("");try{await decidePublication(item.id,item.variants.map(v=>v.id),decision,item.scheduled_at,note);setMessage(decision==="approved"?(item.scheduled_at?"Publication validée et ajoutée au planning.":"Publication validée. Il reste à choisir sa date."):"Corrections demandées à l’équipe.");setNote("");await reload();}catch(error){setMessage(error instanceof Error?error.message:"Action impossible.");}finally{setBusy(false);}}
 return <section className="grid overflow-hidden rounded-2xl border border-subtle bg-surface shadow-sm lg:grid-cols-[340px_1fr]"><aside className="border-b border-subtle lg:border-b-0 lg:border-r"><header className="flex items-center justify-between p-4"><div><h2 className="font-semibold text-ink">À valider</h2><p className="text-xs text-muted">{items.length} publication(s)</p></div><button onClick={()=>void reload()} className="rounded-lg p-2 text-muted hover:bg-interactive"><RefreshCw className="h-4 w-4"/></button></header><div className="divide-y divide-subtle">{items.map(row=><button key={row.id} onClick={()=>setSelected(row.id)} className={`w-full p-4 text-left ${selected===row.id?"bg-primary-soft":"hover:bg-interactive"}`}><p className="text-xs font-semibold text-primary">{row.campaign_title}</p><h3 className="mt-1 font-medium text-ink">{row.title}</h3><p className="mt-2 flex items-center gap-1 text-xs text-muted"><Clock3 className="h-3 w-3"/>{row.scheduled_at?new Date(row.scheduled_at).toLocaleString("fr-FR"):"Date à définir"}</p></button>)}{!items.length?<div className="p-8 text-center text-sm text-muted"><CheckCircle2 className="mx-auto mb-2 h-8 w-8 text-success"/>Aucune publication en attente.</div>:null}</div></aside><main className="min-h-96 p-5">{item?<><header><p className="text-xs font-semibold uppercase tracking-wide text-primary">{item.campaign_title}</p><h2 className="mt-1 text-xl font-semibold text-ink">{item.title}</h2><p className="mt-2 whitespace-pre-wrap text-sm text-muted">{item.content}</p></header><div className="mt-5 grid gap-3 md:grid-cols-2">{item.variants.map(variant=><article key={variant.id} className="rounded-xl border border-subtle p-4"><div className="flex items-center justify-between"><strong className="text-sm text-ink">{labels[variant.network]}</strong><span className="rounded-full bg-warning-soft px-2 py-1 text-xs text-warning-on">À valider</span></div><p className="mt-3 whitespace-pre-wrap text-sm leading-relaxed text-ink">{variant.body}</p></article>)}</div><label className="mt-5 block text-sm font-medium text-ink">Commentaire pour Marie<textarea value={note} onChange={event=>setNote(event.target.value)} rows={3} className="mt-1 w-full rounded-xl border border-subtle bg-surface px-3 py-2.5 text-sm outline-none focus:border-primary" placeholder="Préciser ce qu’il faut modifier…"/></label>{message?<p className="mt-3 rounded-xl bg-interactive p-3 text-sm text-ink">{message}</p>:null}<footer className="mt-4 flex flex-wrap justify-end gap-2"><Button variant="secondary" disabled={busy} onClick={()=>void decide("changes_requested")}><MessageSquareWarning className="h-4 w-4"/>Demander des modifications</Button><Button variant="success" disabled={busy} onClick={()=>void decide("approved")}><Send className="h-4 w-4"/>Valider et programmer</Button></footer></>:<div className="flex h-full min-h-80 flex-col items-center justify-center text-center"><CheckCircle2 className="h-10 w-10 text-success"/><h2 className="mt-3 font-semibold text-ink">Tout est validé</h2><p className="mt-1 text-sm text-muted">Les nouvelles demandes de Marie apparaîtront ici.</p></div>}</main></section>;
}
