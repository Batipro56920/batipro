name: Keep Supabase Active

on:
  schedule:
    - cron: "17 5 * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase auth health
        run: |
          curl --fail --silent --show-error \
            "https://vhwtpwmzaidmlvqcyfep.supabase.co/auth/v1/health"
